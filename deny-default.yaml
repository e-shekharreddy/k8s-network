apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny
  namespace: roboshop
spec:
#deny all incoming request including with in roboshop namespace
  podSelector: {} # all pods in roboshop namespace 
  policyTypes:
  - Ingress